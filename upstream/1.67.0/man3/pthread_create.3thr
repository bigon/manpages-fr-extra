.TH PTHREAD_CREATE 3 "21 juillet 2003" LinuxThreads "Manuel du programmeur Linux"

.SH NOM
pthread_create \- crée un nouveau thread

.SH SYNOPSIS
.B #include <pthread.h>

.BI "int pthread_create(pthread_t * " thread ", pthread_attr_t * " attr ", void * (*" start_routine ")(void *), void * " arg ");"

.SH DESCRIPTION
.B "pthread_create"
crée un nouveau thread s'exécutant simultanément avec le thread appelant. Le
nouveau thread exécute la fonction
.I "start_routine"
en lui passant
.I "arg"
comme premier argument. Le nouveau thread s'achève soit explicitement en
appelant
.BR "pthread_exit" (3),
ou implicitement lorsque la fonction
.I "start_routine"
s'achève. Ce dernier cas est équivalent à appeler
.BR "pthread_exit" (3)
avec la valeur renvoyée par
.I "start_routine"
comme code de sortie.

L'argument
.I "attr"
indique les attributs du nouveau thread. Voir
.BR "pthread_attr_init" (3)
pour une liste complète des attributs. L'argument
.I "attr"
peut être
.BR "NULL" ,
auquel cas, les attributs par défaut sont utilisés\ : le thread créé est
joignable (non détaché) et utilise la politique d'ordonnancement normale (pas
temps-réel).

.SH "VALEUR RENVOYÉE"
En cas de succès, l'identifiant du nouveau thread est stocké à l'emplacement
mémoire pointé par l'argument \fIthread\fP, et 0 est renvoyé. En cas d'erreur,
un code d'errur non nul est renvoyé.

.SH ERREURS
.TP
.B "EAGAIN"
pas assez de ressources système pour créer un processus pour le nouveau thread.
.TP
.B "EAGAIN"
il y a déjà plus de
.B "PTHREAD_THREADS_MAX"
threads actifs.

.SH AUTEUR
Xavier Leroy <Xavier.Leroy@inria.fr>

.SH "VOIR AUSSI"
.BR "pthread_exit" (3),
.BR "pthread_join" (3),
.BR "pthread_detach" (3),
.BR "pthread_attr_init" (3).
.SH TRADUCTION
.RI "Thierry Vignaud <" tvignaud@mandrakesoft.com ">, 2000"

